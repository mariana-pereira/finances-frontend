import React from 'react';

import { Container, NBButton } from './styles';

const LoginHeader = () => (
    <Container>
        <span>Não tem uma conta?<NBButton>Cadastre-se</NBButton></span>
    </Container>
);

export default LoginHeader;